# Database Configuration

This directory contains database initialization scripts for both MongoDB and PostgreSQL.

## MongoDB
- `mongodb/init-mongo.js`: Initializes collections and indexes

## PostgreSQL
- `postgres/init.sql`: Creates tables and indexes

Choose one database system based on your needs. The Docker Compose file is configured to use MongoDB by default.
