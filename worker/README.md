# Worker/Scheduler

This optional component can be used for background tasks such as:
- Sending scheduled notifications
- Running periodic data cleanup
- Processing background jobs

The worker uses Python's `schedule` library for simple task scheduling.
